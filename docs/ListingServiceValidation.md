# ListingServiceValidation

Static utility class providing validation methods for marketplace listing operations. It centralizes validation logic used across listing creation, updates, visibility changes, delisting, featured status toggles, and pagination to ensure consistent data integrity and user feedback.

## API

### `Validate`
